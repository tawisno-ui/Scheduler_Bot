name: Keep Alive

on:
  schedule:
    - cron: "0 0 * * 0" # Every Sunday midnight UTC
  workflow_dispatch:

jobs:
  keepalive:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "Keeping repo active"
